# =============================================================================
# CONFIGURAÇÃO DE PRODUÇÃO - Fluxo de Caixa
# =============================================================================
# IMPORTANTE: Este arquivo é um TEMPLATE. Configure com valores reais em produção
# NUNCA commitar este arquivo com valores reais de produção
# =============================================================================

# Ambiente
NODE_ENV=production

# Servidor
PORT=3000
HOST=0.0.0.0

# Aplicação
APP_NAME=Fluxo de Caixa
APP_VERSION=1.0.0
APP_DESCRIPTION=Sistema de Controle de Fluxo de Caixa

# Sessão (OBRIGATÓRIO ALTERAR EM PRODUÇÃO)
SESSION_SECRET=ALTERAR-PARA-CHAVE-SUPER-SEGURA-EM-PRODUCAO-MIN-32-CHARS
SESSION_MAX_AGE=86400000
SESSION_SECURE=true

# Logging
LOG_LEVEL=info
LOG_TO_FILE=true

# Segurança
ALLOWED_ORIGINS=https://seu-dominio.com,https://www.seu-dominio.com

# Features
ENABLE_DEBUG_ROUTES=false
ENABLE_USER_AUTH=false
ENABLE_FILE_UPLOAD=false
ENABLE_API_EXPORT=true

# Database (configurar se necessário)
# MONGODB_URI=mongodb+srv://usuario:senha@cluster.mongodb.net/fluxo_caixa_prod?retryWrites=true&w=majority

# Upload
MAX_FILE_SIZE=5242880
UPLOAD_PATH=./uploads
ALLOWED_FILE_TYPES=image/jpeg,image/png,application/pdf

# Rate Limiting (mais restritivo em produção)
RATE_LIMIT_WINDOW=900000
RATE_LIMIT_MAX=100
FORM_RATE_LIMIT_MAX=50

# SSL/TLS (se usando HTTPS)
SSL_CERT_PATH=/path/to/certificate.pem
SSL_KEY_PATH=/path/to/private-key.pem

# =============================================================================
# INSTRUÇÕES PARA PRODUÇÃO
# =============================================================================
# 1. Substitua todos os valores marcados com "ALTERAR"
# 2. Configure SESSION_SECRET com pelo menos 32 caracteres aleatórios
# 3. Configure domínios corretos em ALLOWED_ORIGINS
# 4. Configure string de conexão do banco se usar MongoDB
# 5. Verifique se SSL está configurado se usar HTTPS
# 6. Teste todas as configurações em ambiente de staging primeiro
# 7. Configure monitoramento e alertas
# 8. Configure backup automático se usar banco de dados
# =============================================================================

# =============================================================================
# EXEMPLO DE COMANDO PARA GERAR SESSION_SECRET SEGURO:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# =============================================================================