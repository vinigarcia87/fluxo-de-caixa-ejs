<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>

    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">

    <style>
        body {
            background: linear-gradient(135deg, #e3f2fd 0%, #bbdefb 100%);
            min-height: 100vh;
            padding: 40px 0;
        }

        .main-card {
            border: none;
            border-radius: 20px;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
            background: rgba(255, 255, 255, 0.98);
            backdrop-filter: blur(10px);
            overflow: hidden;
        }

        .user-header {
            background: linear-gradient(135deg, #2196f3 0%, #21cbf3 100%);
            color: white;
            margin: -24px -24px 0 -24px;
            padding: 40px 24px;
            text-align: center;
            position: relative;
        }

        .user-photo {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            object-fit: cover;
            border: 4px solid rgba(255, 255, 255, 0.3);
            margin: 0 auto 15px;
        }

        .user-avatar {
            width: 100px;
            height: 100px;
            background: rgba(255, 255, 255, 0.2);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 15px;
            font-size: 36px;
            font-weight: bold;
            border: 4px solid rgba(255, 255, 255, 0.3);
        }

        .info-card {
            border: none;
            border-radius: 15px;
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            margin-bottom: 15px;
            transition: all 0.3s ease;
        }

        .info-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
        }

        .contact-link {
            color: #2196f3;
            text-decoration: none;
            font-weight: 500;
        }

        .contact-link:hover {
            color: #1976d2;
            text-decoration: underline;
        }

        .btn-floating {
            border-radius: 25px;
            padding: 12px 25px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            transition: all 0.3s ease;
        }

        .btn-floating:hover {
            transform: translateY(-2px);
        }

        .stats-badge {
            background: rgba(255, 255, 255, 0.2);
            padding: 8px 15px;
            border-radius: 20px;
            font-size: 12px;
            display: inline-block;
            margin-top: 10px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-7 col-md-9">
                <div class="card main-card">
                    <div class="card-body p-4">
                        <!-- User Header -->
                        <div class="user-header">
                            <% if (user.foto) { %>
                                <img src="/uploads/users/<%= user.foto %>" alt="Foto de <%= user.nome %>" class="user-photo">
                            <% } else { %>
                                <div class="user-avatar">
                                    <%= user.nome.charAt(0).toUpperCase() %>
                                </div>
                            <% } %>
                            <h1 class="h3 fw-bold mb-1"><%= user.nome %></h1>
                            <p class="mb-0 opacity-75">Perfil Completo do Usuário</p>
                            <div class="stats-badge">
                                <i class="bi bi-person-badge me-1"></i>
                                ID: #<%= user.id %>
                            </div>
                        </div>

                        <!-- User Information -->
                        <div class="mt-4">
                            <!-- Email Card -->
                            <div class="card info-card">
                                <div class="card-body p-4">
                                    <div class="row align-items-center">
                                        <div class="col-auto">
                                            <div class="bg-primary text-white rounded-circle d-flex align-items-center justify-content-center"
                                                 style="width: 45px; height: 45px;">
                                                <i class="bi bi-envelope-fill"></i>
                                            </div>
                                        </div>
                                        <div class="col">
                                            <h6 class="fw-bold mb-1 text-muted">Email</h6>
                                            <a href="mailto:<%= user.email %>" class="contact-link h6 mb-0">
                                                <%= user.email %>
                                            </a>
                                        </div>
                                        <div class="col-auto">
                                            <a href="mailto:<%= user.email %>" class="btn btn-outline-primary btn-sm">
                                                <i class="bi bi-send"></i>
                                            </a>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <!-- Phone Card -->
                            <div class="card info-card">
                                <div class="card-body p-4">
                                    <div class="row align-items-center">
                                        <div class="col-auto">
                                            <div class="bg-success text-white rounded-circle d-flex align-items-center justify-content-center"
                                                 style="width: 45px; height: 45px;">
                                                <i class="bi bi-telephone-fill"></i>
                                            </div>
                                        </div>
                                        <div class="col">
                                            <h6 class="fw-bold mb-1 text-muted">Telefone</h6>
                                            <a href="tel:<%= user.telefone.replace(/\D/g, '') %>" class="contact-link h6 mb-0">
                                                <%= user.telefone %>
                                            </a>
                                        </div>
                                        <div class="col-auto">
                                            <a href="tel:<%= user.telefone.replace(/\D/g, '') %>" class="btn btn-outline-success btn-sm">
                                                <i class="bi bi-telephone"></i>
                                            </a>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <!-- CPF Card -->
                            <div class="card info-card">
                                <div class="card-body p-4">
                                    <div class="row align-items-center">
                                        <div class="col-auto">
                                            <div class="bg-warning text-white rounded-circle d-flex align-items-center justify-content-center"
                                                 style="width: 45px; height: 45px;">
                                                <i class="bi bi-credit-card-2-front-fill"></i>
                                            </div>
                                        </div>
                                        <div class="col">
                                            <h6 class="fw-bold mb-1 text-muted">CPF</h6>
                                            <span class="h6 mb-0 text-warning fw-bold"><%= user.cpf %></span>
                                        </div>
                                        <div class="col-auto">
                                            <span class="badge bg-light text-dark">
                                                <i class="bi bi-shield-check me-1"></i>Verificado
                                            </span>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <!-- ID Card -->
                            <div class="card info-card">
                                <div class="card-body p-4">
                                    <div class="row align-items-center">
                                        <div class="col-auto">
                                            <div class="bg-info text-white rounded-circle d-flex align-items-center justify-content-center"
                                                 style="width: 45px; height: 45px;">
                                                <i class="bi bi-hash"></i>
                                            </div>
                                        </div>
                                        <div class="col">
                                            <h6 class="fw-bold mb-1 text-muted">Identificador Único</h6>
                                            <span class="h6 mb-0 text-info fw-bold">#<%= user.id %></span>
                                        </div>
                                        <div class="col-auto">
                                            <span class="badge bg-info text-white">
                                                <i class="bi bi-database me-1"></i>Sistema
                                            </span>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Action Buttons -->
                        <div class="d-flex gap-3 justify-content-center mt-4 pt-3 border-top">
                            <a href="/users/<%= user.id %>/edit" class="btn btn-warning btn-floating">
                                <i class="bi bi-pencil me-2"></i>Editar
                            </a>
                            <a href="/users" class="btn btn-primary btn-floating">
                                <i class="bi bi-arrow-left me-2"></i>Lista
                            </a>
                            <a href="/" class="btn btn-outline-secondary btn-floating">
                                <i class="bi bi-house me-2"></i>Início
                            </a>
                        </div>

                        <!-- Footer -->
                        <div class="text-center mt-4">
                            <small class="text-muted">
                                <i class="bi bi-clock me-1"></i>
                                Perfil atualizado • Dados verificados
                            </small>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap 5 JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>